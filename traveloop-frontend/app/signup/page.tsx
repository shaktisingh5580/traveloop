/**
 * Signup Page — Screen 1b
 * Owner: Pratham (UI) & Shakti (auth integration)
 */

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up",
};

export default function SignupPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      {/* TODO: Build signup form with name, email, password, validation */}
      <h1>Sign Up</h1>
    </div>
  );
}
